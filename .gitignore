# --- Large artifacts: regenerate, don't commit ---
# Cached encoder latents -- rebuild with vjepa/data/collect_*.py
data/
data_*/
datasets/
*.npy
# Trained weights (over GitHub's 100 MB file limit)
checkpoints/*.pt
# Decoder/probe weights are small enough to keep:
!checkpoints/decoders.npz
!checkpoints/grasp_probe.npz

# --- Vendored upstream source ---
# Meta's facebookresearch/vjepa2, cloned by setup.sh. Not redistributed here;
# it carries its own license.
vjepa2_src/

# --- Run logs, scratch, caches ---
logs/
*.log
__pycache__/
*.pyc
# demo videos are small and worth having in the repo
media/grasp_slow.mp4
